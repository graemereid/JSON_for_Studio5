;  - Returns true if psData could be a JSON object because its first character is {
;  IN: psData - JSON to parse
;  OUT: kTrue if psData could be a JSON object
Do method removeWhitespace (psData,kTrue,kFalse) Returns psData
Quit method mid(psData,1,1)=clsObjectStartMarker
;  ---History
;  15-01-18 GBR (ld0008546) Created